name: phoenix
description: "Phoenix - Логистическая платформа"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^12.1.3
  smooth_page_indicator: ^1.2.1
  shared_preferences: ^2.2.2
  flutter_svg: ^2.0.9
  lottie: ^3.0.0
  intl: ^0.19.0
  url_launcher: ^6.2.2
  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true